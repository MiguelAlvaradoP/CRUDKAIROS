<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- 🎨 Fondo con degradado suave -->
    <gradient
        android:startColor="#99FFB9"
        android:centerColor="#99FFB9"
        android:endColor="#99FFB9"
        android:angle="90"/>

    <!-- 🌟 Bordes redondeados -->
    <corners android:radius="18dp"/>

    <!-- 💫 Sombra/borde sutil -->
    <stroke
        android:width="1dp"
        android:color="#086927" />

    <!-- 🧩 Padding interno -->
    <padding
        android:left="16dp"
        android:top="16dp"
        android:right="16dp"
        android:bottom="16dp"/>

</shape>
